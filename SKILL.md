---
name: html-visual-reading
description: 把 blog、论文、综述、网页或一整个工程项目转成单文件、自包含、适合阅读的教学可视化 HTML。用于深度解读一篇文章/论文，或把一个代码仓库讲清楚。触发词包括"做成可视化 html""可视化解读""读一下这篇论文/这个项目""把这个项目讲清楚""visual reading""blog to html"。
---

# HTML 可视化解读

把一篇 blog / paper / survey / 长文，或一整个工程项目，转成**单文件、自包含、适合阅读**的教学可视化 HTML。

**核心原则：帮助读者 get 到材料的核心思想。** 页面首先要流畅、易读，让读者顺着原作者（或原项目）的思路读懂；可视化只是让抽象关系更具体，卡片只是让读者在掌握脉络后自然深入细节。

## 输入输出

**输入**四种形态，取材方法各不相同，见 @references/ingest.md：

| 输入 | 典型场景 | 走哪条专项 |
|---|---|---|
| URL / markdown 路径 / 粘贴文本 | 一篇 blog、一篇论文 | @references/paper.md（论文）／通用流程（blog） |
| arXiv 论文，标题含 survey / review / overview | 综述 | @references/survey.md |
| 本地文件夹（代码仓库、工程项目、skill 目录） | 「把这个项目讲清楚」 | @references/project.md |
| 当前对话已经产出的结论 | 「把刚才聊的做成页面」 | 通用流程，不重新检索 |

**输出**：单个 HTML 文件，打开即用，无构建步骤。

## 硬约束

这几条不随材料变化，先记住：

- **单文件、零外部请求**。CSS 和 JS 全部内联；图标用 emoji 或内联 SVG；字体用字体栈（`Inter, "Noto Sans SC", -apple-system, "PingFang SC", system-ui, sans-serif`），本机有就用、没有就退回系统字体，**不引 CDN**。本地图片 base64 内联，远程图片按 @references/ingest.md 的规则校验后直引 URL。
- **中文**。正文 / 标题 / 导航 / 按钮 / 提示文案一律中文，原文是英文时要翻译，**禁止整页堆英文**。仅这几种情况保留英文原文：专有名词（人名、公司、产品、论文标题）、代码 / 命令 / API、需要原汁原味的金句引用（此时配中文译文或简短解释）。
- **不出现「TL;DR」这个字面词**。三层渐进披露的结构照用，但第一层叫「核心结论」或直接写成一句话判断，不叫 TL;DR。
- **源链接必须显式引用**。输入提供了原文 URL（或文内可提取到权威源链接）时：页头或核心结论附近放一个「阅读原文 ↗」，页脚再出现一次并附作者 / 站点名。文中引用的其他外链资源也保留跳转。项目场景同理——链回仓库地址。参考样式：[anthropic-multi-agent-research-system](https://syt-nju.github.io/files/visual-reading/anthropic-multi-agent-research-system/)。

## 默认读者画像

除非用户另有说明，默认读者是**熟悉 LLM 的后训练工程师**：

- 可以直接使用 rollout、train / validation / test split、reward、agent loop、tool calling、harness、context window、SFT、RL 等常见术语，不需要刻意改写成小白语言
- 材料新定义的术语、非标准缩写、含义重叠的概念，以及在材料中有特殊含义的常见词，必须在第一次出现时解释
- 技术表达优先保证概念边界准确；不要为了「通俗」把多个不同概念压成同一个中文词
- 英文术语比生硬中文翻译更准确时，采用「英文术语 + 一句中文定义」，后文保持同一用词

材料面向明显不同的读者群，或用户指定了读者画像时，以用户要求为准。**读者画像是第 2 步读者契约的输入，不是替代品**——每次都要写清这次的读者已经知道什么、还不知道什么。

---

## 流程

### 第 1 步：取材

按输入形态取材，见 @references/ingest.md。同时**动手画图前先找现成的图**——原作者、原仓库、原论文大概率已经画好了架构图、pipeline 图，取图优先级和校验方法同样在 ingest.md。

### 第 2 步：定契约（写 HTML 前必做）

在设计 section 或组件之前，先完成**四份契约 + 主线契约**，见 @references/contracts.md：

1. **读者契约** —— 读者已经知道什么、还不知道什么
2. **术语契约** —— 3–8 个承担核心推理作用的术语，逐个定死中文表述、一句话定义、与相邻概念的边界
3. **数据流契约** —— 每个阶段读取什么、修改什么、输出什么
4. **Running Example 契约** —— 一个贯穿方法部分的具体例子
5. **主线契约（最高优先级）** —— 把全文压缩成一个核心问题，每个默认展开的 section 只能承担五种职责之一

契约不一定原样出现在页面上，但必须指导全文。**契约写完停下来交给用户确认，得到明确同意再往下写。** 这一步花五分钟，能挡住整页重写。

### 第 3 步：设计一条流畅阅读路径

```text
先抓住主旨 → 顺着正文理解脉络 → 用图把抽象关系具体化 → 用卡片/表格逐步深入证据和细节
```

三层由浅入深：

```text
第 1 层：核心结论 + 核心思想          ← 30 秒知道材料在讲什么
第 2 层：连续正文 + 少量关键图解       ← 3–5 分钟读懂脉络
第 3 层：卡片、表格、原话、延伸细节    ← 按需深读
```

具体 section 顺序、数量、命名**完全由材料决定**，不套固定模板。好的 section 标题应该像文章大纲，不像组件清单。走论文 / 综述 / 项目专项时，各自的叙事主线见对应 reference。

**长文阅读底线**：

- 连续正文是骨架，图解和卡片是辅助
- 每个复杂图解前必须有自然过渡，说明它为什么出现
- 导航命名应反映读者的理解路径，而不只是组件名
- 如果读者需要按顺序理解，就不要一上来堆术语或架构图；先交代为什么这件事重要
- 把所有可展开区域保持折叠时，页面仍必须完整讲清主旨；读者不能被迫点击卡片来拼接主线

### 第 4 步：为每块内容选可视化手段

pattern 库、选择要点、流程图契约见 @references/visual-patterns.md。核心判断：

- 先判断这块内容是主线、证据、补充解释，还是可选细节
- 一屏至多一个「视觉锚点」（大动画或大图），其余用轻量手段
- 并列关系用卡片，链式关系用流程图，层级关系用树，数字单独拎出来
- 图必须回答一个具体问题：谁先发生、谁依赖谁、反馈回到哪里、什么时候进入下一步
- 动画必须承载信息变化；只是让点沿线移动就删掉，改成静态图和清晰标签

### 第 5 步：写页面

视觉与交互规范、配色基线、实现陷阱见 @references/visual-patterns.md；中文行文规范见 @references/writing-style.md。

**风格底线：克制、低饱和、信息优先。** 不做装饰性炫技，不靠颜色堆砌存在感；强调用字重 / 字号 / 留白，不用颜色。

### 第 6 步：验证（必做，这步省不掉）

静态层、运行时层、内容层、读者理解检查，逐条见 @references/verify.md。**跳过运行时验证是这个 skill 最常见的失败方式**——静态检查通过不代表 JS 能跑。

### 第 7 步：交付

- 把验证结果如实报告：跑过哪几层、发现并修了什么、哪条没跑成（比如本机没有浏览器自动化工具，Console 检查需要用户自己看）
- 列 2–3 个可迭代方向，不主动写 README
- 顺手起一个本地 server 方便阅读：在 HTML 所在目录执行 `python3 -m http.server 8000`，把访问地址告诉用户。**给局域网 IP 下的地址，不要给 localhost**（用 `ipconfig getifaddr en0` 取本机 IP）

---

## 反面教训

写之前和验证时各扫一遍 @references/anti-patterns.md。踩得最多的五条：

1. **模板优先而非内容优先** —— 拿着固定模板塞内容
2. **中文双引号混 ASCII 双引号** —— JS 直接 parse 失败，页面看起来是静态的
3. **只做静态检查不开浏览器** —— Lint 过不代表 JS 能跑
4. **把长文切成卡片墙** —— 卡片承载补充细节，不能取代连续正文
5. **没有读者画像就开始写** —— 常见术语解释过度、材料专属术语解释不足，同时发生

## 心法

> **先问「读者读完应该 get 到什么」，再问「怎样读最顺」，最后才问「用什么视觉呈现能帮他 get 到」。**
>
> 页面结构是答案，不是前提。
